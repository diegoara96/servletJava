node {
        stage('Build') {
               checkout scm
        }
        stage('Test') { 
                echo test 
        }
        stage('Deploy') { 
               echo deploy 
            }
}
